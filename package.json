{
  "name": "develogger",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.2",
    "@libsql/client": "^0.15.10",
    "hono": "^4.8.5",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}